p
    a href="/games" Return to the startsite
p
    a href="/games/#{@game['id']}" Return to #{@game["name"]}

h2 #{@game["name"]} - #{@category["name"]}

p Select an article:

ol
- @articles.each do |article|
    li
        a href="#" #{article["name"]}

h3 Admin 

p Change the category's name:
form action="/categories/#{@category['id']}/update" method="post" 
    input type="text" name="new_name" placeholder="New name" autocomplete="off"/
    input type="submit" value="Update"/

p Delete category:
form action="/categories/#{@category['id']}/delete" method="post" 
    input type="submit" value="Delete category"/
    input type="hidden" name="game_id" value="#{@game['id']}"/